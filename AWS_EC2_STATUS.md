# 🖥️ AWS EC2 部署狀態報告

**生成時間**：2026-01-13
**區域**：ap-northeast-1 (Tokyo)

---

## 📊 EC2 實例總覽

你目前有 **3 個運行中的 EC2 實例**：

| # | 名稱 | 實例 ID | 類型 | 狀態 | 公網 IP | 密鑰對 |
|---|------|---------|------|------|---------|--------|
| 1 | **tango_1_1** | i-0b29ca671375dad53 | t4g.nano | ✅ 運行中 | 13.113.155.16 | bn-watcher-key |
| 2 | **ploy-tandgo-1-1-jp** | i-01de34df55726073d | t3.micro | ✅ 運行中 | 3.112.247.26 | ploy-jp-key |
| 3 | **s-ui-proxy** | i-02cd10231bbd1be17 | t3.small | ✅ 運行中 | 13.230.45.157 | s-ui-proxy-key |

---

## 🎯 實例詳細信息

### 1. tango_1_1 ⭐（推薦部署目標）

**基本信息**：
- **實例 ID**：i-0b29ca671375dad53
- **實例類型**：t4g.nano（ARM 架構，2 vCPU，0.5 GB RAM）
- **狀態**：✅ 運行中
- **啟動時間**：2026-01-13 01:13:28 UTC（今天剛啟動）

**網絡信息**：
- **公網 IP**：13.113.155.16
- **私有 IP**：172.31.23.51
- **安全組**：bn-watcher-sg

**訪問信息**：
- **SSH 密鑰**：bn-watcher-key
- **連接方式**：
  ```bash
  # 使用 SSM（推薦）
  aws ssm start-session --target i-0b29ca671375dad53

  # 使用 SSH（需要密鑰）
  ssh -i ~/.ssh/bn-watcher-key.pem ubuntu@13.113.155.16
  ```

**部署狀態**：
- ✅ 已準備好部署 Ploy Trading System
- ✅ 部署腳本已創建
- ⏳ 等待執行部署

**推薦用途**：
- 🎯 **Ploy Trading System 主要部署目標**
- 適合運行前端 + 輕量級後端
- 成本低（t4g.nano 約 $3.5/月）

---

### 2. ploy-tandgo-1-1-jp

**基本信息**：
- **實例 ID**：i-01de34df55726073d
- **實例類型**：t3.micro（x86 架構，2 vCPU，1 GB RAM）
- **狀態**：✅ 運行中
- **啟動時間**：2026-01-03 12:35:03 UTC（運行 10 天）

**網絡信息**：
- **公網 IP**：3.112.247.26
- **私有 IP**：172.31.34.59
- **安全組**：bn-watcher-sg

**訪問信息**：
- **SSH 密鑰**：ploy-jp-key
- **連接方式**：
  ```bash
  # 使用 SSM
  aws ssm start-session --target i-01de34df55726073d

  # 使用 SSH（需要密鑰）
  ssh -i ~/.ssh/ploy-jp-key.pem ubuntu@3.112.247.26
  ```

**當前用途**：
- 可能已經部署了 Ploy 相關服務
- 運行時間較長，可能有現有配置

**推薦用途**：
- 備用部署目標
- 測試環境
- 或保持現有配置不變

---

### 3. s-ui-proxy

**基本信息**：
- **實例 ID**：i-02cd10231bbd1be17
- **實例類型**：t3.small（x86 架構，2 vCPU，2 GB RAM）
- **狀態**：✅ 運行中
- **啟動時間**：2026-01-06 06:21:31 UTC（運行 7 天）

**網絡信息**：
- **公網 IP**：13.230.45.157
- **私有 IP**：172.31.30.68
- **安全組**：s-ui-proxy-sg

**訪問信息**：
- **SSH 密鑰**：s-ui-proxy-key
- **連接方式**：
  ```bash
  # 使用 SSM
  aws ssm start-session --target i-02cd10231bbd1be17

  # 使用 SSH（需要密鑰）
  ssh -i ~/.ssh/s-ui-proxy-key.pem ubuntu@13.230.45.157
  ```

**當前用途**：
- 從名稱看，可能用作代理服務器
- 配置了獨立的安全組

**推薦用途**：
- 保持現有用途（代理服務）
- 不建議用於 Ploy 部署

---

## 💰 成本估算

| 實例 | 類型 | 每月成本（估算）| 用途 |
|------|------|----------------|------|
| tango_1_1 | t4g.nano | ~$3.5 | Ploy Trading（推薦）|
| ploy-tandgo-1-1-jp | t3.micro | ~$7.5 | 備用/測試 |
| s-ui-proxy | t3.small | ~$15 | 代理服務 |
| **總計** | - | **~$26/月** | - |

*注：成本基於 Tokyo 區域的按需定價，實際成本可能因使用時間和數據傳輸而異*

---

## 🎯 部署建議

### 推薦方案：部署到 tango_1_1

**理由**：
1. ✅ 今天剛啟動，環境乾淨
2. ✅ 成本最低（t4g.nano）
3. ✅ 已經準備好部署腳本
4. ✅ 適合運行前端 + 輕量級後端

**部署步驟**：
```bash
# 1. 運行一鍵部署腳本
./deploy_quick.sh

# 2. 選擇選項 1（SSM 自動部署）

# 3. 等待部署完成

# 4. 訪問
# http://13.113.155.16
```

### 備選方案：部署到 ploy-tandgo-1-1-jp

**理由**：
- 更多內存（1 GB vs 0.5 GB）
- 可能已有相關配置
- x86 架構，兼容性更好

**注意**：
- 需要先檢查現有配置
- 可能需要清理或遷移現有服務

---

## 🔍 檢查現有部署

讓我們檢查這些實例上是否已經有部署：

### 檢查 tango_1_1

```bash
aws ssm start-session --target i-0b29ca671375dad53

# 在 EC2 上執行
ls -la ~/
ps aux | grep -E "nginx|ploy|node"
sudo systemctl list-units --type=service --state=running
```

### 檢查 ploy-tandgo-1-1-jp

```bash
aws ssm start-session --target i-01de34df55726073d

# 在 EC2 上執行
ls -la ~/
ps aux | grep -E "nginx|ploy|node"
sudo systemctl list-units --type=service --state=running
```

---

## 📋 快速命令參考

### 連接到實例

```bash
# tango_1_1
aws ssm start-session --target i-0b29ca671375dad53

# ploy-tandgo-1-1-jp
aws ssm start-session --target i-01de34df55726073d

# s-ui-proxy
aws ssm start-session --target i-02cd10231bbd1be17
```

### 管理實例

```bash
# 停止實例（節省成本）
aws ec2 stop-instances --instance-ids i-0b29ca671375dad53

# 啟動實例
aws ec2 start-instances --instance-ids i-0b29ca671375dad53

# 查看實例狀態
aws ec2 describe-instances --instance-ids i-0b29ca671375dad53 \
  --query 'Reservations[0].Instances[0].[State.Name,PublicIpAddress]' \
  --output text
```

### 查看所有實例

```bash
# 列出所有實例
aws ec2 describe-instances \
  --query 'Reservations[*].Instances[*].[InstanceId,State.Name,InstanceType,PublicIpAddress,Tags[?Key==`Name`].Value|[0]]' \
  --output table

# 只列出運行中的實例
aws ec2 describe-instances \
  --filters "Name=instance-state-name,Values=running" \
  --query 'Reservations[*].Instances[*].[InstanceId,InstanceType,PublicIpAddress,Tags[?Key==`Name`].Value|[0]]' \
  --output table
```

---

## 🎯 下一步行動

### 立即部署到 tango_1_1（推薦）

```bash
# 1. 運行部署腳本
./deploy_quick.sh

# 2. 部署完成後訪問
# http://13.113.155.16
```

### 或者先檢查現有部署

```bash
# 連接到 ploy-tandgo-1-1-jp 檢查
aws ssm start-session --target i-01de34df55726073d

# 查看是否已有 Ploy 部署
ls -la ~/ploy/
sudo systemctl status ploy-backend nginx
```

---

## 📊 總結

**你的 AWS EC2 部署狀況**：
- ✅ 3 個運行中的實例
- ✅ tango_1_1 已準備好部署 Ploy Trading System
- ✅ 部署腳本和文檔已完成
- ⏳ 等待執行部署

**推薦行動**：
1. 部署到 **tango_1_1**（最新、最乾淨）
2. 運行 `./deploy_quick.sh`
3. 訪問 http://13.113.155.16

**備選行動**：
1. 檢查 **ploy-tandgo-1-1-jp** 的現有配置
2. 決定是否遷移或重新部署

---

**版本**：v1.0.0
**日期**：2026-01-13
**狀態**：✅ EC2 實例運行中，準備部署
