{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DeploymentApplyRequest",
  "type": "object",
  "required": [
    "bundle_id",
    "deployment_id",
    "desired_state",
    "runtime_mode"
  ],
  "properties": {
    "account_id": {
      "default": "default",
      "type": "string"
    },
    "bundle_id": {
      "type": "string"
    },
    "deployment_id": {
      "type": "string"
    },
    "deployment_state": {
      "default": "enabled",
      "allOf": [
        {
          "$ref": "#/definitions/DeploymentState"
        }
      ]
    },
    "desired_state": {
      "$ref": "#/definitions/DesiredState"
    },
    "max_gross_exposure": {
      "default": null,
      "type": [
        "string",
        "null"
      ],
      "pattern": "^-?[0-9]+(\\.[0-9]+)?$"
    },
    "runtime_mode": {
      "$ref": "#/definitions/DeploymentRuntimeMode"
    }
  },
  "definitions": {
    "DeploymentRuntimeMode": {
      "type": "string",
      "enum": [
        "paper",
        "live"
      ]
    },
    "DeploymentState": {
      "type": "string",
      "enum": [
        "enabled",
        "draining",
        "disabled",
        "archived"
      ]
    },
    "DesiredState": {
      "type": "string",
      "enum": [
        "running",
        "paused",
        "stopped"
      ]
    }
  }
}
