//! Placeholder parquet analysis module.
//!
//! The `analysis` feature still exposes this module path in `strategy::mod`,
//! but the implementation was split out in earlier refactors. Keep a minimal
//! module here so formatter and feature-gated builds can resolve the path.
