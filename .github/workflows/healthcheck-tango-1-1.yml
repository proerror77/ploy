name: Healthcheck tango-1-1

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

permissions:
  contents: read

jobs:
  healthcheck:
    name: Verify tango-1-1 runtime health
    runs-on: ubuntu-latest
    steps:
      - run: echo "healthcheck probe"
