name: Healthcheck tango-1-1-v2

on:
  workflow_dispatch:

concurrency:
  group: healthcheck-tango-1-1-v2
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  healthcheck:
    name: Verify tango-1-1 runtime health
    runs-on: ubuntu-latest
    steps:
      - name: probe
        run: echo "healthcheck v2 probe"
