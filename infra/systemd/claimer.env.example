# Optional overrides for the auto-claimer systemd unit.
#
# Put real secrets in /opt/ploy/.env or /opt/ploy/env/claimer.env on the host.
# Do NOT commit secrets to git.
#
# Required (one of):
# POLYMARKET_PRIVATE_KEY=0x...
# PRIVATE_KEY=0x...
#
# Optional (proxy wallet):
# POLYMARKET_FUNDER=0x...

# Optional: minimum native gas required by claimer signer (wei).
# Default: 5000000000000000 (0.005 POL/MATIC)
# CLAIMER_MIN_NATIVE_GAS_WEI=5000000000000000

# Optional: auto top-up signer gas from a dedicated wallet.
# Disabled by default for safety.
# Enable only when you understand the transfer limits below.
# CLAIMER_AUTO_TOPUP_ENABLED=true
# CLAIMER_AUTO_TOPUP_PRIVATE_KEY=0x...   # top-up wallet private key (must hold POL/MATIC)
# CLAIMER_AUTO_TOPUP_THRESHOLD_WEI=5000000000000000
# CLAIMER_AUTO_TOPUP_TARGET_WEI=20000000000000000
# CLAIMER_AUTO_TOPUP_MAX_PER_TX_WEI=20000000000000000
# CLAIMER_AUTO_TOPUP_DAILY_CAP_WEI=100000000000000000
# CLAIMER_AUTO_TOPUP_RESERVE_WEI=5000000000000000

# Optional: gasless redeem via Polymarket relayer + builder credentials.
# Enabled by default in code path, but only activates when credentials exist.
# When active, claimer skips native gas preflight and submits redeem through relayer.
#
# CLAIMER_RELAYER_ENABLED=true
# CLAIMER_RELAYER_URL=https://relayer-v2.polymarket.com
# CLAIMER_BUILDER_API_KEY=00000000-0000-0000-0000-000000000000
# CLAIMER_BUILDER_SECRET=base64-encoded-secret==
# CLAIMER_BUILDER_PASSPHRASE=hex-or-string-passphrase
#
# Optional: if true, fallback to direct on-chain redeem when relayer fails.
# Keep false for strict gasless mode.
# CLAIMER_RELAYER_FALLBACK_ONCHAIN=false
#
# Optional relayer poll tuning:
# CLAIMER_RELAYER_MAX_POLLS=100
# CLAIMER_RELAYER_POLL_INTERVAL_MS=2000

# Optional: ignore redeemable conditions by id (comma/space separated).
# Useful for stale/zero-value conditions that keep appearing in Data API.
# Prefix match is supported, so short condition-id prefixes are enough.
# CLAIMER_IGNORE_CONDITION_IDS=0x04e8fab12c2e30,0x1b2990a7a04d91
# (alias: CLAIMER_IGNORE_CONDITIONS)
