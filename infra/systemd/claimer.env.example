# Optional overrides for the auto-claimer systemd unit.
#
# Put real secrets in /opt/ploy/.env or /opt/ploy/env/claimer.env on the host.
# Do NOT commit secrets to git.
#
# Required (one of):
# POLYMARKET_PRIVATE_KEY=0x...
# PRIVATE_KEY=0x...
#
# Optional (proxy wallet):
# POLYMARKET_FUNDER=0x...

# Optional: minimum native gas required by claimer signer (wei).
# Default: 5000000000000000 (0.005 POL/MATIC)
# CLAIMER_MIN_NATIVE_GAS_WEI=5000000000000000

# Optional: auto top-up signer gas from a dedicated wallet.
# Disabled by default for safety.
# Enable only when you understand the transfer limits below.
# CLAIMER_AUTO_TOPUP_ENABLED=true
# CLAIMER_AUTO_TOPUP_PRIVATE_KEY=0x...   # top-up wallet private key (must hold POL/MATIC)
# CLAIMER_AUTO_TOPUP_THRESHOLD_WEI=5000000000000000
# CLAIMER_AUTO_TOPUP_TARGET_WEI=20000000000000000
# CLAIMER_AUTO_TOPUP_MAX_PER_TX_WEI=20000000000000000
# CLAIMER_AUTO_TOPUP_DAILY_CAP_WEI=100000000000000000
# CLAIMER_AUTO_TOPUP_RESERVE_WEI=5000000000000000
